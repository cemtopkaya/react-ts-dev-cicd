// Jest DOM matchers'ı yükler (React Testing Library ile kullanılır)
import '@testing-library/jest-dom';

console.log('setupTests.ts çalıştı');

// fetch API'yi mock'lamak için bir kütüphane
// import 'whatwg-fetch';

// Testler için global bir mock örneği
// globalThis.myGlobalFunction = () => {
//   console.log('Bu bir global test fonksiyonudur.');
// };